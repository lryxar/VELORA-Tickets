const config=require('../utils/config'); const perms=require('../utils/permissions'); const tickets=require('../utils/tickets'); const logger=require('../utils/logger');
module.exports={name:'interactionCreate',async execute(interaction){ try{ if(interaction.isChatInputCommand()){ const cmd=interaction.client.slashCommands.get(interaction.commandName); if(!cmd) return; const allowed=config.get('permissions').slash?.[interaction.commandName]||['admin']; if(!perms.has(interaction.member,allowed)) return interaction.reply({content:config.get('messages').noPermission,ephemeral:true}); await logger.log(interaction.client,'commandUsage',`/${interaction.commandName} used by ${interaction.user.tag}`); return cmd.execute(interaction); } if(interaction.isButton()){ if(interaction.customId.startsWith('ticket_open_')) return tickets.create(interaction, interaction.customId.replace('ticket_open_','')); if(interaction.customId==='ticket_claim'){ const allowed=config.get('permissions').prefix?.claim||['staff','admin']; if(!perms.has(interaction.member,allowed)) return interaction.reply({content:config.get('messages').noPermission,ephemeral:true}); tickets.update(interaction.channel.id,{claimedBy:interaction.user.id}); await logger.log(interaction.client,'claim',`${interaction.user.tag} claimed ${interaction.channel.name}`); return interaction.reply({content:`Claimed by <@${interaction.user.id}>`}); } if(interaction.customId==='ticket_close'){ const allowed=config.get('permissions').prefix?.close||['staff','admin']; if(!perms.has(interaction.member,allowed)) return interaction.reply({content:config.get('messages').noPermission,ephemeral:true}); await interaction.reply({content:'Closing ticket...'}); return tickets.close(interaction.channel); } } }catch(e){ logger.error(e); if(interaction.isRepliable()) interaction.reply({content:config.get('messages').error,ephemeral:true}).catch(()=>{}); }} };
