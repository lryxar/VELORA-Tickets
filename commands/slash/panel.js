const { SlashCommandBuilder, EmbedBuilder, ActionRowBuilder, ButtonBuilder }=require('discord.js'); const config=require('../../utils/config'); const tickets=require('../../utils/tickets');
module.exports={data:new SlashCommandBuilder().setName('panel').setDescription('Send a ticket panel.').addStringOption(o=>o.setName('type').setDescription('Panel type').setRequired(true).addChoices({name:'support',value:'support'},{name:'purchase',value:'purchase'})),async execute(interaction){ const type=interaction.options.getString('type'); const p=config.panel(type); const e=new EmbedBuilder().setColor(p.embedColor).setTitle(p.panelTitle).setDescription(p.description).setFooter({text:p.footer||'VELORA Tickets'}); if(p.image) e.setImage(p.image); if(p.thumbnail) e.setThumbnail(p.thumbnail); const b=new ButtonBuilder().setCustomId(`ticket_open_${type}`).setLabel(p.buttonLabel).setStyle(tickets.style(p.buttonStyle)); if(p.buttonEmoji) b.setEmoji(p.buttonEmoji); await interaction.channel.send({embeds:[e],components:[new ActionRowBuilder().addComponents(b)]}); await interaction.reply({content:`${type} panel sent.`,ephemeral:true}); }};
