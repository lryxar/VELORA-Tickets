const { SlashCommandBuilder, EmbedBuilder }=require('discord.js'); const config=require('../../utils/config');
module.exports={data:new SlashCommandBuilder().setName('help').setDescription('Shows available commands.'),async execute(interaction){ const bot=config.get('bot'); const m=config.get('messages'); const e=new EmbedBuilder().setColor(bot.embedColors?.primary||'#7C3AED').setTitle(m.helpTitle).addFields({name:'Slash Commands',value:'`/panel support`\n`/panel purchase`\n`/help`\n`/ping`'},{name:'Ticket Commands',value:'`+claim`, `+unclaim`, `+rename`, `+add`, `+remove`, `+close`, `+delete`, `+transcript`, `+move`, `+info`'},{name:'Bot Information',value:'JSON configured, lightweight, Termux compatible.'}); await interaction.reply({embeds:[e],ephemeral:true}); }};
